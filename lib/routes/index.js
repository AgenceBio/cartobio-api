'use strict'

const sandboxSchema = {
  schema: {
    tags: ['Bac à sable']
  }
}

const ocSchema = {
  schema: {
    tags: ['Organisme de certification']
  }
}

const internalSchema = {
  schema: {
    tags: ['cartobio.org']
  }
}

module.exports = {
  sandboxSchema,
  ocSchema,
  internalSchema
}
